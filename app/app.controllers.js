'use strict';

/* Controllers */

var BBTControllers = angular.module('BBTControllers', []);


BBTControllers.controller('StartCtrl', ['$scope',
  function($scope) {
  }]);

BBTControllers.controller('RandomCtrl', ['$scope',
  function($scope) {
  }]);

BBTControllers.controller('PDFCtrl', ['$scope',
  function($scope) {
  }]);

BBTControllers.controller('H1Ctrl', ['$scope',
  function($scope) {
  }]);


